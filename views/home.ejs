<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Url Short</title>
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h1>URL Shortener</h1>
            </div>
        </div>
    <form action="/create" method="POST">
        <div class="row">
            
                <div class="input-group mb-3">
                    <input type="text" name="longurl" class="form-control" placeholder="Paste Your long URL"
                        aria-label="Recipient's username" aria-describedby="button-addon2">
                    <div class="input-group-append">
                        <input type="submit" class="btn btn-outline-secondary" value="Submit">
                    </div>
                </div>
            
        </div>
    </form>
        <div class="row">
            <% for(let i=0;i< UrlResult.length;i++) {%>
            <div class="col-lg-3 d-flex">
                <div class="card border-secondary flex-fill mb-3" style="max-width: 18rem;">
                    <div class="card-header">Total Click: <%=UrlResult[i].clickCount  %></div>
                    <div class="card-body text-secondary d-flex flex-column">
                        <a href="http://localhost:3000/<%=UrlResult[i].shortUrl  %>" target="_blank"><%=UrlResult[i].shortUrl  %></a>
                        <p class="card-text"><%=UrlResult[i].longUrl  %></p>
                        <a href="/qrcode/<%=UrlResult[i].shortUrl  %>" class="btn btn-primary mt-auto d-flex justify-content-center align-items-center"  role="button">View QR Code</a>
                        <br>
                        <form method="POST" action="/delete/<%= UrlResult[i].shortUrl %>" style="display: inline;">
                            <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this URL?')">Delete</button>
                        </form>
                        
                    </div>
                </div>
            </div>
            <% } %>
           
           
        </div>
    </div>


</body>

</html>